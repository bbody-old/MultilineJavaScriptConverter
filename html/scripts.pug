each script in includes.scripts
	script(type='text/javascript' src= script)
each script in includes.scripts_async
	script(type='text/javascript' src= script async)
if production
	each script in includes.scripts_async_production
		script(type='text/javascript' src= script async)
include ./analytics.pug

script.
	window.onload=function(){
		// Initialize clipboard editing
		new ClipboardJS('#copy');
	}
